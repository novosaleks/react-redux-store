const initialState = {
    bookList: {
        books: [],
        loading: true,
        error: null,
    },
    cart: {
        cartList: [],
        total: 100241,
    }
};

export default initialState;